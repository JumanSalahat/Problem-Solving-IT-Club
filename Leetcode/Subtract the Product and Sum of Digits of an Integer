class Solution {
public:
    int subtractProductAndSum(int n) {
        long long sum=0;
        long long product=1;
        while(n)
        {
          sum+=n%10;
          product*=n%10;
          n/=10;
        }
         return (product-sum);
    }
};
